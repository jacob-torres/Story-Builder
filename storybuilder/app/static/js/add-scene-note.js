// Add a note to a scene
const sceneNoteForm = document.getElementById('scene-note-form');

// Toggle visibility of the form
function toggleSceneNoteForm() {
    sceneNoteForm.style.display = sceneNoteForm.style.display === 'none' ? 'block' : 'none';
}